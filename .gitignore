# Build artifacts
/out/
/dist/
coverage.out
*.test
# The compiled CLI binary at the repo root (anchored so it does not match the
# cmd/sysgo source directory).
/sysgo

# Editor/OS
.DS_Store
.idea/
.vscode/
